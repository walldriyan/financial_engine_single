use crate::core::errors::EngineResult;

/// ============================================================================
/// 🔌 Plugin Trait (ප්ලගින ගුණාංග)
/// ============================================================================
/// ඕනෑම කෙනෙකුට එන්ජිමට අලුත් දේවල් එකතු කිරීමට මෙය භාවිතා කළ හැක.

pub trait Plugin {
    fn name(&self) -> &str;
    fn on_load(&self) -> EngineResult<()>;
    fn on_unload(&self) -> EngineResult<()>;
}
