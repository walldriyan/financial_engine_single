name: 🚀 Deploy to Google Cloud Run

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: my-financial-engine-project # TODO: ඔබේ Project ID එක මෙතනට දාන්න (හෝ Secrets පාවිච්චි කරන්න)
  REGION: asia-south1 # Mumbai (Lankaවට ළඟම)
  SERVICE_NAME: financial-engine

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      # 1. Google Cloud වෙත Log වෙන්න (Github Secrets ඕනෙ)
      - id: 'auth'
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      # 2. Cloud SDK පිහිටුවන්න
      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v2'

      # 3. Docker Build & Push & Deploy (Cloud Build භාවිතා කර)
      - name: '🚀 Build and Deploy'
        run: |
          gcloud builds submit --config cloudbuild.yaml .
